# v5.0.2 Legacy Log Migration

Added:
- Detects old logs from legacy path:
  teams/{teamId}/workouts
- Migrates legacy logs into isolated user path:
  teams/{teamLabel}/users/{userUid}/workouts
- Manual confirmation required.
- Migration copies only; it does not delete old logs.
- Adds migratedFromLegacy, legacyId, migratedAt, ownerUid, ownerEmail metadata.

Preserved:
- v5.0.1 user isolation fix
- Login / Firestore / Save / Delete
- Dashboard / Log / Calendar / Coach / Program / Guide / Donate
