Workout PRO v5.3.6 Final Lite Patch

This patch finalizes the lightweight performance improvements for Workout PRO by synchronizing all visible version strings, removing the legacy Migration Tool, and retaining the date/save optimizations. It should be deployed after testing to ensure all pages display the correct version and run smoothly.
