# v5.0.8 Migration Binding Hard Fix

Fixed:
- Migration button still did not trigger the migration check.
- Exposes module functions to window:
  - window.checkLegacyLogsForMigration
  - window.migrateLegacyLogsToUser
  - window.bindLegacyMigration
- Runtime captures Migration button click directly.
- If module is not ready, the migration box shows a clear error instead of staying silent.

Preserved:
- v5.0.7 Migration UI cleanup
- v5.0.6 Null Guard Fix
- v5.0.4 Team ID Hard Fix
- v5.0.1 User Isolation
