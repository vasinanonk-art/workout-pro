# v5.2.1 Local Date Fix

Fixed:
- Log Workout default date showed previous day after midnight in Thailand.
- Replaced date-only UTC usage:
  new Date().toISOString().slice(0,10)
  with local browser date helper.
- Kept createdAt / updatedAt / exportedAt as UTC timestamps.

Preserved:
- v5.2.0 Backup Export Tool
- v5.1.5 Post Save Safe Callback Fix
- Calendar Month Safe Fix
- Migration completed flow
- User isolation
- Firestore save flow
